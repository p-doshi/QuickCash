package com.example.csci3130_group_3;

// Unit tests can debug the logic
// So they're the best choice for checking if location is enabled
public class LocationUnitTest {
    // Android doesn't let you mock location processes for safety reasons.
    // This means Unit Tests are pointless, I'll just use UI tests.
}